using System;

namespace apertureLabsRoboticArm
{
    class Program
    {
        static void Main(string[] args)
        {
            Router r = new Router();
            r.Run();
        }
    }
}
// The 'Main' loop
  // It continuously executes the code you put in there.

// Instantiate it only once.
// Move the Router instatiation out of the main.

// Work out how to use the debugger.
// Put it in the main loop.
// Find the memory address of the router class, write it down.  Then open the debugger
// again and check to see if the memory.

// Ruby and Javascript have one but it is hidden

